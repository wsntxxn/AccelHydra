defaults:
  - default
  - _self_

train_data_list:
  - ${..star_audiocaps_vits_direct.train}

val_data_list:
  - ${..star_audiocaps_vits_direct.val}

test_data_list:
  - ${..star_audiocaps_vits_direct.test}